---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "datakit.fullname" . }}
  {{- with .Values.serviceAccountAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
