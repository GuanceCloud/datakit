
log: ft.log
log_level: debug
installdir: .

binlog:
  disable: false
  jobs:
  - ft_gateway: "http://localhost:9528/v1/write/metrics"
    addr: "192.168.56.20:3306"
    user: "aaa"
    password: ""

    inputs:
    - database: test
      tables:
      - table: t1
        measurement: t1
        columns:
          fa: field
          ff: field
          fb: tag
          fc: tag
          fd: tag
          fe: tag
          ft: tag
      #  exclude_events:
      #  - delete
      #- table: table2
      #  measurement: table2
      #  columns:
      #    field1: field
      #    tag1: tag
      #exclude_tables:
      #- table3
